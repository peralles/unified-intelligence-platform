# OAuth / secrets — nunca versionar
credentials.json
token.json
token-*.json
service_account.json
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Cursor — estado local (não compartilhar entre máquinas)
.cursor/hooks/state/

# Runtime / dados locais
data/
!data/.gitkeep
*.db
*.sqlite
